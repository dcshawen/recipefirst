# Development Dockerfile for Vue.js frontend with Vite hot-reload
FROM node:20-slim

WORKDIR /app

# Copy package files
COPY www/package.json ./

# Install dependencies
RUN npm install

# Copy source code
COPY www/ .

# Expose Vite dev server port
EXPOSE 5173

# Start Vite dev server with hot reload
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
