# Usage Notes

- The development server can be started by running the following command from the data/ directory
	`fastapi dev server.py
- The autorefresh takes a minute to detect a file change and reload the dev server so there is a 1 to 2 minute delay until changes will take effect